services:
  db:
    image: postgres:16-alpine
    container_name: tumige-bot-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: tumige_bot_db
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    restart: always
